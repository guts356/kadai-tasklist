<h1>タスク・ステータス一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %>タスク:<%= task.content %>&emsp;ステータス:<%= task.status %></li>
  <% end %>
</ul>
<%= link_to '新規タスクの投稿', new_task_path %>